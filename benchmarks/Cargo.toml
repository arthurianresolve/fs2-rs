[package]
name = "fs2-benchmarks"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
criterion = "0.8.2"
fs2 = { path = ".." }
tempfile = "3.27"

[[bench]]
name = "fs2"
harness = false
